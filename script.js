let minedAmount = 0;

document.getElementById('mineButton').addEventListener('click', function() {
    minedAmount += 1; // Increase mined amount by 1 for each click
    document.getElementById('minedAmount').innerText = 'Mined Amount: ' + minedAmount;
});

document.getElementById('showAdsButton').addEventListener('8449907', function() {
    // Insert your ad code here
    const adCode = `
        <div style="width: 300px; margin: auto; border: 1px solid #ccc; padding: 10px;">
            <h3>Ad Title</h3>
            <p>This is an ad content.</p>
        </div>
    `;
    
    // Create an element to display the ad
    const adContainer = document.createElement('8449907');
    adContainer.innerHTML = adCode;
    document.body.appendChild(8449907);
});
